server:
  port: 5000
spring:
  application:
    name: admin
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml

management:
  endpoints:
    web:
      exposure:
        include: '*'
