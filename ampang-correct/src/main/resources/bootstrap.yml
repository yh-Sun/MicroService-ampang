server:
  port: 7030
spring:
  application:
    name: correct-a
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
  main:
    allow-bean-definition-overriding: true
nacos:
  server-addr: localhost:8848

management:
  endpoints:
    web:
      exposure:
        include: '*'
